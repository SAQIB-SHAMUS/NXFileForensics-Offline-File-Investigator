# NXFileForensics-Offline-File-Investigator
NXFileForensics — An offline, zero-dependency CLI for investigating files on your machine. Check if a file's real type matches its extension, find duplicates, spot high-entropy (encrypted/packed) content, extract strings, and diff files byte-by-byte — all locally, no network calls, no APIs.
